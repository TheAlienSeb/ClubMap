"use client";//reder via client side directive

 export const mapAPI = () => {
    console.log("mapAPI componnent being mounted via client");

    
    return(
        <>
        
        </>
    );
}