export function handleLogInSubmit(e) {
    e.preventDefault();
  
    const form = e.target;
    const username = form[0].value;
    const password = form[1].value;

    /*Check  database if this exists, if it doesnt return an error for wrong password/username*/

    alert("Not Built Yet...")
}